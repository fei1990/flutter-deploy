# frozen_string_literal: true

RSpec.describe Flutter::Deploy do
  it "has a version number" do
    expect(Flutter::Deploy::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
